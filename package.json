{
  "homepage": "https://sundrun.github.io/sjoveisreglene-vikeplikt",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
